// import {
//
// } from './types';

export default function(state={}, action) {
  switch(action.type) {
    // case CLEAR_UPDATE_USER_DATA:
    //   return {
    //     ...state,
    //     updateUser: action.payload,
    //   }
    default:
      return state;
  }
}
